{
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "tsc-lint": "turbo run tsc-lint",
    "test": "turbo run test",
    "ci:test": "turbo run tsc-lint && turbo run test"
  }
}
